import 'package:flutter/material.dart';

class InheriteWidgetMain extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(
        "XZQ",
        style: Theme.of(context).textTheme.title,
      ),
    );
  }
}
